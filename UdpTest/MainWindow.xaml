<Window x:Class="UdpTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UDP 测试工具 - 用于测试 NetMeter 的 UDP 限制" Height="580" Width="620"
        WindowStartupLocation="CenterScreen" Background="#F5F6F8">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="测试模式">
            <StackPanel Orientation="Horizontal" Margin="8">
                <RadioButton x:Name="ModeFlows" Content="连接数测试" IsChecked="True" GroupName="Mode" Checked="Mode_Changed"/>
                <RadioButton x:Name="ModeSend" Content="上行限速测试" Margin="16,0,0,0" GroupName="Mode" Checked="Mode_Changed"/>
                <RadioButton x:Name="ModeRecv" Content="下行限速测试" Margin="16,0,0,0" GroupName="Mode" Checked="Mode_Changed"/>
            </StackPanel>
        </GroupBox>

        <Grid Grid.Row="1" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="ParamLabel" Grid.Column="0" Text="连接数量:" VerticalAlignment="Center" Foreground="#444444"/>
            <TextBox x:Name="ParamBox" Grid.Column="1" Text="10" Width="80" Margin="6,0,0,0" VerticalContentAlignment="Center"/>
            <TextBlock Grid.Column="2" Text="秒数:" Margin="16,0,0,0" VerticalAlignment="Center" Foreground="#444444"/>
            <TextBox x:Name="SecondsBox" Grid.Column="3" Text="60" Width="60" Margin="6,0,0,0" VerticalContentAlignment="Center"/>
            <TextBlock Grid.Column="4" Text="并发数:" Margin="16,0,0,0" VerticalAlignment="Center" Foreground="#444444"/>
            <TextBox x:Name="FlowsBox" Grid.Column="5" Text="4" Width="60" Margin="6,0,0,0" IsEnabled="False" VerticalContentAlignment="Center"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
            <Button x:Name="StartButton" Content="开始" Width="110" Padding="0,6" Click="Start_Click"/>
            <TextBlock Text="当前速率:" Margin="24,0,0,8" VerticalAlignment="Bottom" Foreground="#666666"/>
            <TextBlock x:Name="RateText" Text="0.0 KB/s" FontSize="22" FontWeight="Bold" Foreground="#1565C0" VerticalAlignment="Bottom"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Text="日志:" Margin="0,12,0,0" Foreground="#444444"/>

        <TextBox Grid.Row="4" x:Name="LogBox" IsReadOnly="True" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12" Margin="0,4,0,0"
                 Background="White" BorderBrush="#DDDDDD"/>
    </Grid>
</Window>